aqui ira as informacoes

git checkout main
git pull origin main
git merge Recreacao---Components

# Se houver conflitos, resolva-os, então:
git add .
git commit

# Finalmente, empurre as mudanças para o repositório remoto
git push origin main

